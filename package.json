{
  "name": "universal-blockchain-mcp",
  "version": "1.0.0",
  "description": "Universal Blockchain MCP server for cross-chain development with ZetaChain, Foundry, and multi-chain support",
  "main": "dist/index.js",
  "module": "./src/index.ts",
  "type": "module",
  "scripts": {
    "build": "npx @smithery/cli build",
    "dev": "npx @smithery/cli dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "ethers": "^6.13.0",
    "zetachain": "^6.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}